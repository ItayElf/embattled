export default interface DamageCalcRes {
  max_damage: number;
  min_damage: number;
  avg_damage: number;
  max_casualties: number;
  min_casualties: number;
  avg_casualties: number;
  min_turns_to_kill: number;
  max_turns_to_kill: number;
}
